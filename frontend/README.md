# YieldMind Frontend

Next.js dashboard for the YieldMind vault on Mantle Testnet.

```shell
npm install
npm run dev
```

The dashboard reads the deployed vault address from `contract.ts` and uses Wagmi with chain ID `5003`.
